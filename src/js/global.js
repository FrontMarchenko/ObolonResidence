export const WIN = $(window);
export const DOC = $(document);
export const BODY = $('body');

export const HEADER = $('.js-header');

export const BREAKPOINTS = {
  mobileLG: window.matchMedia('(max-width: 1023px)')
};